name: gha-demo1

on:
  push:
    branches: [ main ]
    paths-ignore: [ .github/** ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: "15 6 * * 0"
  workflow_dispatch:
    inputs:
      environment:
        description: 'Env to deploy to'
        type: environment
        required: true

jobs:
  Build:
    runs-on: ubuntu-latest
    environment: DEV
    needs: ListFile
    steps:
      - run: |
          echo "The job was triggered by event: ${{ github.event_name }}"
          echo "The name of your branch is ${{ github.ref }} and your repo is ."
          echo "Display NEV_NAME from Environment: ${{ vars.ENV_NAME_DEV }} "
          echo "job name: ${{ vars.JOB_NAME }}"
          echo "repo level variable: ${{ vars.REPO_LEVEL_VAR1_NAME }}"
          env
          
  Test:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    needs: ListFile
    environment: Test
    steps:
      - run: |
          echo "🧪 Testing..."
          sleep 15

  Prod:
    runs-on: ubuntu-latest
    environment: Production
    needs: [Build, Test]
    if: github.event.inputs.environment == 'Production'
    steps:
      - run: |
          echo "🚀 Step 1..."
          sleep 10
      - run: |
          echo "🚀 Step 2..."
          sleep 10
      - run: |
          echo "🚀 Step 3..."
          sleep 10
      - run: |
          echo "🚀 Step 4..."
          sleep 10
      - run: |
          echo "🚀 Step 5..."
          sleep 10    

      
  ListFile:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source file
        uses: actions/checkout@v3.3.0

      - name: List files in the repo
        run: |
          echo "The repo ${{ github.repository }} contains the following files:"
          tree              
